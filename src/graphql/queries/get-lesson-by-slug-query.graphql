query GetLessonBySlug ($slug: String) {
    lesson(where: {slug: $slug}) {
        teacher {
        name
        bio
        avatarURL
        }
        title
        videoId
        description
    }
}